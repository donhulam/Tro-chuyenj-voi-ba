import React from 'react';

const ApiKeyModal: React.FC = () => {
  // Per @google/genai coding guidelines, the API key must be an environment variable.
  // This component should not be used to prompt the user for an API key.
  // It is provided as an empty component to satisfy project structure and resolve compilation errors.
  return null;
};

export default ApiKeyModal;
